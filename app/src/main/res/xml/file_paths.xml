<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Dossier pour les images temporaires de la caméra -->
    <external-cache-path name="camera_images" path="." />

    <!-- Dossier pour les images de l'application -->
    <external-files-path name="app_images" path="Pictures" />

    <!-- Dossier interne pour les images -->
    <files-path name="internal_images" path="images/" />

    <!-- Cache interne -->
    <cache-path name="cache_images" path="images/" />
</paths>